# Fixture — a Claude configuration directory with no live session

The registry is `sessions/`, a directory of `<pid>.json` files, one per live session.
Here it is empty: nothing runs, so nothing can be joined. Git keeps no empty directory,
hence the `.gitkeep`.
